import 'package:flutter/material.dart';

class LoginPageService extends StatelessWidget {
  const LoginPageService({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
